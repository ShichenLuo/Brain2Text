# Artifacts

Local outputs belong here and should not be committed:

- `checkpoints/`: trained PyTorch weights.
- `reports/`: optional experiment exports and evaluation summaries.

For reproducibility, record the checkpoint, dataset version, random seed, device,
and decoder settings alongside published metrics.
